const add = (x, y) => {
    return x + y;
};
const sample = (arr) => {
    const idx = (Math.random() * arr.length);
    return arr[idx];
};
const x = 1;
export const add2 = (x, y) => {
    return x + y;
};
export const sample2 = (arr) => {
    const idx = (Math.random() * arr.length);
    return arr[idx];
};
